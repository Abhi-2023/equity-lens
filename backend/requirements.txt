fastapi>=0.120
uvicorn[standard]>=0.30
sse-starlette>=2.1
python-dotenv>=1.0
pydantic>=2.7

# DB (Postgres via asyncpg — see Section 8, "Cloud SQL (Postgres)")
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29

# Cache
redis>=5.0

# Scheduling (Section 2.4/7 — watchlist auto-refresh; stands in for Cloud Scheduler)
apscheduler>=3.10

# Agent orchestration
langgraph>=1.1
langchain-core
langchain-groq
langchain-mcp-adapters

# MCP
mcp>=1.0

# RAG
qdrant-client[fastembed]==1.19.0  # pinned in lockstep with the qdrant/qdrant image tag in docker-compose.yml
rank-bm25>=0.2
cohere>=5.0

# Observability
langsmith>=0.1

# Tools
yfinance>=0.2
ddgs>=1.0
requests>=2.31

# Testing
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
